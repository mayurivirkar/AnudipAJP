Create Table Employee(id,name varchar(30));
@Entity
public class Employee {

@id-this creates the
    primary key
    in database use of'@id'
    int id;
    string name;
    // getter
    // setter
}